<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itt.t00154755.mouseapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
    <!-- The application needs these permissions in order to function correctly -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
     

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!--
        This is the main Activity here the user will check to
        see if blue-tooth is enabled, if not the user will be prompted
        to grant permission to application in order for the application
        to enable the blue-tooth
        -->
        <activity
            android:name=".App"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
         This is the dialog box that appears whenever the connect
         option is selected from the menu.
        -->
        <activity
            android:name=".CheckBTDevices"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Dialog" >
            <intent-filter>
                <action android:name="itt.t00154755.mouseapp.CHECKBTAVAILABILITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
         <!--
         This is the dialog box that appears whenever the connect
         option is selected from the menu.
        -->
        <activity
            android:name=".Prefs"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Dialog" >
            <intent-filter>
                <action android:name="itt.t00154755.mouseapp.PREFS" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service android:name=".AccelometerService"/>
        <receiver android:name=".AcceloDataReceiver" android:enabled="true">
  <intent-filter>
     <action android:name="itt.t00154755.AccelometerService.ACCELEROMETER_DATA"></action>
  </intent-filter>
</receiver>
    </application>

</manifest>