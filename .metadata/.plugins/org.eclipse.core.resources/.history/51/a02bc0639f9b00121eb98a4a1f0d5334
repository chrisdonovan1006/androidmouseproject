<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itt.t00154755.mouseapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
	<!-- The application needs these permissions in order to function correctly -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!-- This is the main Activity here the user will check to
        see if blue-tooth is enabled, if not the user will be prompted
        to grant permission to application in order for the application
        to enable the blue-tooth -->
        <activity
            android:name=".App"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- This is the main app activity -->
        <activity
            android:name=".CheckBTAvailability"
            android:label="@string/app_name" >
        </activity>
        <!-- This is the App Client -->
        <activity
            android:name=".AppClientService"
            android:label="@string/app_name" >
        </activity>
        <!-- This is the App Client Communication Thread -->
        <activity
            android:name=".ClientCommsThread"
            android:label="@string/app_name" >
        </activity>
        <!-- This is the App Calibrate Threshold Task -->
        <activity
            android:name=".CalibrateThresholdTask"
            android:label="@string/app_name" >
        </activity>
        
		<!-- ++++++++++++++++++++++++++++++++++++++++++ These are used to add preferences to the application ++++++++++++++++++++++++++++++++++++++++++ -->
        <!-- This is a dialog box that shows up on top of the main activity
        	it display information about the Check Bluetooth Activity.
        	That is the reason for the Intent FIlter -->
        <activity
            android:name=".AboutCheckBT"
            android:label="@string/app_name" 
            android:theme="@android:style/Theme.Dialog">
            <intent-filter>
                <action android:name="itt.t00154755.mouseapp.ABOUTCHECKBT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- This is a default preference Activity it currently uses
        	a deprecated method, i need to update this file...
        	That is the reason for the Intent FIlter -->        
        <activity
            android:name=".Prefs"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="itt.t00154755.mouseapp.PREFS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
    </application>

</manifest>