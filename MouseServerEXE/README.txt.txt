Android BluetoothMouse Application Server

This server is written and complied in pure Java using JDK_7.

In order to get the application to run double click on the Executable JAR file.
Then select the start server option in order to set the server to listening mode.
To stop the server select the stop server option.